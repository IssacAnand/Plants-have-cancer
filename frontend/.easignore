# Local JavaScript dependencies
node_modules

# Local Python environment
venv

# Git metadata and local Expo state
.git
.expo

# Android build outputs
android/build
android/app/build

# Backend lives at ../backend/ — outside the frontend build root, so no exclusions needed
.claude